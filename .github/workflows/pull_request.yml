name: Pulumi
on:
  - pull_request
jobs:
  preview:
    name: Preview
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
      - uses: docker://pulumi/actions
        with:
          args: preview
        env:
          # AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          # AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          # PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}
          AKIAYUAE22TSIMNKPZ5R
          AWS_ACCESS_KEY_ID: AKIAYUAE22TSIMNKPZ5R
          AWS_SECRET_ACCESS_KEY: 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
          PULUMI_ACCESS_TOKEN: pul-ccb6cca5e41a514e35cd4f3e48f0b98d7bf67967
          PULUMI_CI: pr